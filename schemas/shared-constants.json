{
  "$comment": "Cross-language shared constants (#425) — single source of truth. Frontend imports this file directly (src/store/systemGroups.ts, src/lib/scalardbIdentifier.ts, src/lib/schemas/reportDefinition.ts); the server mirrors the values in SharedConstants.java, pinned by SharedConstantsTest (repo-root relative read, same pattern as TaxRatesTest). Change values here first — drift fails the server test.",
  "systemGroupProductMaster": "__productMaster__",
  "dbIdentifierPattern": "^[a-zA-Z_][a-zA-Z0-9_]*$"
}
